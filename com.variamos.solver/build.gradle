apply plugin: 'java'
apply plugin: 'eclipse'
archivesBaseName = 'com.variamos.solver'
version = '1.0-SNAPSHOT'

dependencies {
    
    compile project(':com.variamos.common')
    compile project(':com.variamos.hlcl')
}

test {
	environment "LD_LIBRARY_PATH", project.JPL_LIB_PATH

}